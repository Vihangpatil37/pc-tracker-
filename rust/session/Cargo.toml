[package]
name = "session"
version = "0.1.0"
edition = "2021"

[dependencies]
tracing = "0.1"
serde = { version = "1", features = ["derive"] }
chrono = "0.4"
collector = { path = "../collector" }
database = { path = "../database" }
